const assert = require('assert');
const mongoose = require('mongoose');
const User = require('../src/user');
const Comment = require('../src/coment');
const BlogPost = require('../src/blogPost');

describe('Associations', () => {
  it('', done => {
    
  })
})